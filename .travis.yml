sudo: required
addons:
  chrome: stable
language: node_js
node_js:
- node
script:
- npm run test-ci
- npm run coverage-ci
- npm run bundle
deploy:
  provider: npm
  skip_cleanup: true
  edge: true
  email: $NPM_EMAIL
  api_key: $NPM_API_KEY
  on:
    tags: true
    repo: arusak/record-player
